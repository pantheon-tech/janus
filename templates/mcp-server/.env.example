# MCP servers run over stdio. Most config travels via the CLI args
# in the user's claude settings — only secrets/runtime tunables go here.

# --- Optional: API key for backend services this server calls ---
# UPSTREAM_API_KEY=
