// MCP server tool tests live in tests/server.test.ts.
// This stub overrides the shared archetype placeholder; add integration tests here if needed.
import { describe, it } from 'vitest';

describe('mcp-server', () => {
  it.todo('add integration tests here — see server.test.ts for unit tests');
});
