@AGENTS.md

## Claude-specific

- Use Sonnet for spawned agents unless task explicitly needs Opus.
- Invoke `/plan` before non-trivial changes.
- Use skills where they apply: `/fix`, `/reconcile`, `/git-clean`.
