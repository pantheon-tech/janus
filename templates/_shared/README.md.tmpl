{{=<% %>=}}
# <%workload%>

<%description%>

## Prerequisites

- Node <%node_version%> (`.nvmrc` pinned)
- pnpm 10+
- Azure CLI (`az`) for deploys
- GitHub CLI (`gh`) for repo operations

## Quick Start

```bash
pnpm install
cp .env.example .env.local
# fill in .env.local values
pnpm dev
```

## Commands

```bash
pnpm dev           # run locally
pnpm test          # run tests
pnpm lint          # biome check
pnpm format        # biome format --write
pnpm typecheck     # tsc --noEmit
pnpm build         # production build
```

## Deploy

```bash
pnpm deploy:staging  # see infra/deploy.sh
```

Branch model: push to `staging` deploys staging; merge to `main` deploys prod
(via GitHub Environment with required reviewers).

Infrastructure is managed via Bicep + AVM composition. See `infra/README.md`.

## Project docs

- [AGENTS.md](./AGENTS.md) — primary agent context
- [docs/architecture.md](./docs/architecture.md) — system overview
- [docs/conventions/](./docs/conventions/) — shared conventions snapshot from janus
- [docs/adr/](./docs/adr/) — architectural decisions
- [docs/runbooks/](./docs/runbooks/) — operational procedures

## License

<%license%> — see [LICENSE](./LICENSE).
