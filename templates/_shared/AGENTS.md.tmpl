{{=<% %>=}}
# <%workload%>

<%description%>

## Tech Stack

| Aspect | Choice |
|---|---|
| Package manager | pnpm |
| Runtime | Node <%node_version%> |
| Language | TypeScript (strict) |
| Lint + format | Biome v2 |
| Tests | Vitest |
| Cloud | Azure (staging, <%region%>) |
| IaC | Bicep + AVM composition |
| Archetype | <%archetype%> |

## Commands

```bash
pnpm install
pnpm dev
pnpm test
pnpm lint
pnpm typecheck
pnpm build
pnpm deploy:staging   # ./infra/deploy.sh staging
```

## Branching

- `staging` — working branch, deploys to staging on push
- `main` — production, deploys to prod (manual / required reviewers)

## Structure

```
<TODO: populated by scaffold per archetype>
```

## Critical Context

- <TODO: non-obvious invariants specific to this project>

## Design Decisions

- See `docs/conventions/` for shared conventions (stack, layering, testing, etc.)
- See `docs/adr/` for project-specific architectural decisions.

## Known Issues

- <dated list>

---

*Last reviewed: <%date%>*
*Template: janus <%template_version%>*
