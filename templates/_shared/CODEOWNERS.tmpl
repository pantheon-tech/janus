{{=<% %>=}}
# CODEOWNERS — GitHub uses this to auto-request reviews.
#
# `@<value>` works for both users and orgs, but if the slot value is an org
# you usually want a team handle: `@<org>/<team-slug>`. Edit this file after
# scaffolding if the default below doesn't match your repo's actual owner.
* @<%github_org%>
